
/**
 * ThemedContainer - DEPRECATED
 * 
 * Este componente não é mais necessário.
 * O tema agora é aplicado via CSS variables no SSR (layout.tsx)
 * 
 * Use classes Tailwind diretamente:
 * - bg-background, bg-card, bg-muted
 * - text-foreground, text-muted-foreground
 * - text-brew-black, text-brew-black-foreground
 * - border-border
 * 
 * Exemplo:
 * <div className="bg-background text-foreground">
 *   <h1 className="text-brew-black">Título</h1>
 *   <p className="text-muted-foreground">Descrição</p>
 * </div>
 */

export {}

// Mantido apenas para referência durante migração
// Pode ser removido após migrar todos os componentes
